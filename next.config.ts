import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["192.168.1.22", "kathlyn-nonimplicative-believingly.ngrok-free.dev"]
};

export default nextConfig;
